{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMakeBuildTask",
            "type": "shell",
            "command": "cmake --build build --config Release",
            "group": "build"
        }
    ]
}